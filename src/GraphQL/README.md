# Server

> URL /graphql

```graphql
query {
    ping
    directus {
        version
        plugins {
            # TBD
        }
    }
    environment {
        name
        server
        container
        os
    }

}
```


# Project

> URL /{project}/graphql

```graphql


```
